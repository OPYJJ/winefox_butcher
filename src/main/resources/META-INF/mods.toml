# Winefox Maid Butcher mod metadata
modLoader="javafml"
loaderVersion="[47,)"
license="MIT / CC BY-NC-SA 4.0"
# A URL to refer people to when problems occur with this mod
#issueTrackerURL="https://change.me.to.your.issue.tracker.example.invalid/" #optional
# If your mod is purely client-side and has no multiplayer functionality (be it dedicated servers or Open to LAN),
# set this to true, and Forge will set the correct displayTest for you and skip loading your mod on dedicated servers.
#clientSideOnly=true #optional - defaults to false if absent
# A list of mods - how many allowed here is determined by the individual mod loader
[[mods]] #mandatory
# The modid of the mod
modId="winefox_butcher" #mandatory
# The version number of the mod
version="0.10" #mandatory
# A display name for the mod
displayName="Winefox Maid Butcher" #mandatory
# A URL to query for updates for this mod. See the JSON update specification https://docs.minecraftforge.net/en/latest/misc/updatechecker/
#updateJSONURL="https://change.me.example.invalid/updates.json" #optional
# A URL for the "homepage" for this mod, displayed in the mod UI
#displayURL="https://change.me.to.your.mods.homepage.example.invalid/" #optional
# A file name (in the root of the mod JAR) containing a logo for display
#logoFile="examplemod.png" #optional
# A text field displayed in the mod UI
credits="OPYJJ and an anonymous co-author (original joint development)" #optional
# A text field displayed in the mod UI
authors="OPYJJ" #optional
# Display Test controls the display for your mod in the server connection screen
# MATCH_VERSION means that your mod will cause a red X if the versions on client and server differ. This is the default behaviour and should be what you choose if you have server and client elements to your mod.
# IGNORE_SERVER_VERSION means that your mod will not cause a red X if it's present on the server but not on the client. This is what you should use if you're a server only mod.
# IGNORE_ALL_VERSION means that your mod will not cause a red X if it's present on the client or the server. This is a special case and should only be used if your mod has no server component.
# NONE means that no display test is set on your mod. You need to do this yourself, see IExtensionPoint.DisplayTest for more information. You can define any scheme you wish with this value.
# IMPORTANT NOTE: this is NOT an instruction as to which environments (CLIENT or DEDICATED SERVER) your mod loads on. Your mod should load (and maybe do nothing!) whereever it finds itself.
#displayTest="MATCH_VERSION" # if nothing is specified, MATCH_VERSION is the default when clientSideOnly=false, otherwise IGNORE_ALL_VERSION when clientSideOnly=true (#optional)

# Access Transformers
# Optional, if not specified Forge will attempt to find the default META-INF/accesstransformer.cfg and silently continue if not found. May default to an empty list in a future MC.
# If you specify AT path strings in this list, Forge will attempt to load each of them and throw errors for any that aren't found.
# Specifying an empty list when your mod has no ATs is recommended to opt-out of the default for slightly better loading performance.
#accessTransformers = []

# The description text for the mod (multi line!) (#mandatory)
description='''A linkage addon between Touhou Little Maid, Butchercraft and GeckoLib: winefox cuisine, the two-block roast fox feast, maid breeding and villager conversion.'''
# A dependency - use the . to indicate dependency for a specific modid. Dependencies are optional.
[[dependencies.winefox_butcher]] #optional
    # the modid of the dependency
    modId="forge" #mandatory
    # Does this dependency have to exist - if not, ordering below must be specified
    mandatory=true #mandatory
    # The version range of the dependency
    versionRange="[47,)" #mandatory
    # An ordering relationship for the dependency - BEFORE or AFTER required if the dependency is not mandatory
    # BEFORE - This mod is loaded BEFORE the dependency
    # AFTER - This mod is loaded AFTER the dependency
    ordering="NONE"
    # Side this dependency is applied on - BOTH, CLIENT, or SERVER
    side="BOTH"
# Here's another dependency
[[dependencies.winefox_butcher]]
    modId="minecraft"
    mandatory=true
    # This version range declares a minimum of the current minecraft version up to but not including the next major version
    versionRange="[1.20.1,1.21)"
    ordering="NONE"
    side="BOTH"

# Touhou Little Maid dependency
[[dependencies.winefox_butcher]]
    modId="touhou_little_maid"
    mandatory=true
    versionRange="[1.5.3,1.6)"
    ordering="AFTER"
    side="BOTH"

# Butchercraft dependency
[[dependencies.winefox_butcher]]
    modId="butchercraft"
    mandatory=true
    versionRange="[2.4.1,3)"
    ordering="AFTER"
    side="BOTH"

# GeckoLib dependency
[[dependencies.winefox_butcher]]
    modId="geckolib"
    mandatory=true
    versionRange="[4.8.4,5)"
    ordering="AFTER"
    side="BOTH"

# Farmer's Delight (optional, used for the cooking recipes)
[[dependencies.winefox_butcher]]
    modId="farmersdelight"
    mandatory=false
    versionRange="[1.3.2,2)"
    ordering="AFTER"
    side="BOTH"

# Love & Loathing / callresponse (optional, used for the grave compat)
[[dependencies.winefox_butcher]]
    modId="callresponse"
    mandatory=false
    versionRange="[2.0.4,3)"
    ordering="AFTER"
    side="BOTH"
